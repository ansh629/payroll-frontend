{
  "name": "payroll-system-frontend",
  "version": "1.0.0",
  "description": "Frontend for payroll management system",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",  // Starts the development server
    "build": "react-scripts build",  // Bundles the app for production
    "test": "react-scripts test",    // Runs tests
    "eject": "react-scripts eject"   // Ejects configuration for customization
  },
  "dependencies": {
    "axios": "^1.3.0",  // For making HTTP requests to the backend
    "react": "^18.2.0",  // React core library
    "react-dom": "^18.2.0",  // React DOM for rendering
    "react-scripts": "^5.0.1",  // Create React App build tools
    "react-router-dom": "^6.4.3"  // For routing between different pages/components
  },
  "devDependencies": {
    "react-error-overlay": "^6.0.9"  // For error overlays in development (optional)
  },
  "author": "Your Name",
  "license": "MIT",
  "homepage": "http://localhost:3000",  // The homepage for the local server
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
